{
  "scripts": {
    "watch:sass": "sass --watch assets/sass:_site/css",
    "build:sass": "sass assets/sass:_site/css",
    "watch:eleventy": "npx @11ty/eleventy --serve",
    "build:eleventy": "npx @11ty/eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@contentful/rich-text-html-renderer": "^15.13.1",
    "contentful": "^9.1.32",
    "dotenv": "^16.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.53.0"
  }
}
